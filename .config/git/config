[user]
	email = JPEWhacker@gmail.com
	name = Joshua Watt
[push]
	default = simple
[color]
	ui = auto
[sendemail]
	from = Joshua Watt <JPEWhacker@gmail.com>
	smtpEncryption = tls 
	smtpServer = smtp.gmail.com 
	smtpUser = JPEWhacker@gmail.com 
	smtpServerPort = 587
	confirm = always
[include]
	path = ~/.gitconfig.local
[diff]
	tool = meld
[difftool]
	prompt = false
[difftool "meld"]
	cmd = meld "$LOCAL" "$REMOTE"
[merge]
	tool = meld
	conflictstyle = diff3
[mergetool "meld"]
	cmd = meld "$LOCAL" "$BASE" "$REMOTE" --output "$MERGED" --diff "$BASE" "$LOCAL" --diff "$BASE" "$REMOTE" --auto-merge
[core]
	editor = vimx
	hooksPath = ~/.config/git/hooks/
[url "ssh://git@push.yoctoproject.org/poky-contrib"]
    pushInsteadOf = git://git.yoctoproject.org/poky-contrib
[url "ssh://git@push.yoctoproject.org/meta-mingw-contrib"]
    pushInsteadOf = git://git.yoctoproject.org/meta-mingw-contrib
[url "ssh://git@push.yoctoproject.org/meta-mingw"]
    pushInsteadOf = git://git.yoctoproject.org/meta-mingw
[pretty]
    quote = %h (\"%s\")
[sendpatch "meta-mingw"]
    args = -s --to=yocto@yoctoproject.org --subject-prefix=yocto][meta-mingw][PATCH
[sendpatch "oe-core"]
    args = -s --to=openembedded-core@lists.openembedded.org --subject-prefix=OE-core][PATCH
[sendpatch "meta-poky"]
    args = -s --to=poky@yoctoproject.org --subject-prefix=poky][meta-poky][PATCH
[sendpatch "bitbake"]
    args = -s --to=bitbake-devel@lists.openembedded.org --subject-prefix=bitbake-devel][PATCH
[mutt]
    startfolder = =patches/to-apply


